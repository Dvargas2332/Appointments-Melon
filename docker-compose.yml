version: "3.9"
services:
  db:
    image: postgres:16
    container_name: melon_db
    environment:
      POSTGRES_USER: melon
      POSTGRES_PASSWORD: melon
      POSTGRES_DB: melon
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
